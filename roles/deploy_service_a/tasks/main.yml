---
- name: Deploy service_a
  os_server:
    state: present
    name: acme_service_a
    image: Ubuntu server 16.04
    key_name: "{{ keyname }}"
    wait: yes
    flavor: "{{ default_flavor }}"
    auto_floating_ip: no
    network: "{{ companyname }}_LAN"
    security_groups:
      - ssh
      - webservice
    meta:
      hostname: acme_service_a.localdomain
  register: acme_service_a

- name: Add VM to inventory
  add_host:
    name: acme_service_a
    hostname: "{{acme_service_a.server.public_v4}}"
    ansible_host: "{{acme_service_a.server.public_v4}}"
